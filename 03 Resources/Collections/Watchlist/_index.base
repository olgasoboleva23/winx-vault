formulas:
  coverImageFormula: file.embeds[0]
views:
  - type: cards
    name: Table
    filters:
      and:
        - type == "watch"
    order:
      - title
      - media_type
      - status
      - tags
    image: formula.coverImageFormula
